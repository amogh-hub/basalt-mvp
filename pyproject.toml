[project]
name = "basalt-mvp"
version = "2.1.0a1"
description = "Basalt v2.1 Alpha Knowledge Platform: persistent AST-anchored project graphs, graph freshness, impact analysis, task-specific context compilation, and proof-first repository verification."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
basalt = "basalt_proof.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
