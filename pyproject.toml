[project]
name = "basalt-mvp"
version = "2.0.0a1"
description = "Basalt v2.0 Alpha Proof Platform: sandboxed repository verification, mutation testing, security and dependency scanning, proof scoring, CI gates, dashboards, and PR-ready evidence."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
basalt = "basalt_proof.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
