[project]
name = "basalt-mvp"
version = "2.2.0a1"
description = "Basalt v2.2 Alpha Safe Fix Platform: governed agent-assisted patch proposals, capability policies, human approval tokens, atomic state transactions, proof comparison, rollback, and Phase 2 repository intelligence."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
basalt = "basalt_proof.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
